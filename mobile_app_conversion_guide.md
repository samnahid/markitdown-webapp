# MarkItDown-কে ১০০% অফলাইন অ্যান্ড্রয়েড অ্যাপে (APK) রূপান্তর করার গাইডলাইন

শামীম আহমেদ নাহিদ ভাই, আপনার চাহিদা অনুযায়ী **কোনো অনলাইন হোস্টিং বা সার্ভার ছাড়াই সম্পূর্ণ অফলাইনে** আপনার অ্যান্ড্রয়েড ফোনে অ্যাপটি চালানোর জন্য একটি পূর্ণাঙ্গ গাইডলাইন নিচে দেওয়া হলো। 

এই পদ্ধতিতে আপনার মোবাইল ফোনের ভেতরেই ব্যাকগ্রাউন্ডে পাইথন ফ্লাস্ক সার্ভারটি চালু হবে এবং অ্যাপের ভেতরেই ফাইল কনভার্ট করার সব কাজ সম্পন্ন হবে। এর জন্য কোনো ইন্টারনেট সংযোগের প্রয়োজন নেই।

---

## 🏗️ মূল আর্কিটেকচার (How It Works Offline)
আমরা প্রজেক্টের রুটে একটি `main.py` এবং `buildozer.spec` ফাইল তৈরি করেছি।
* **`main.py`:** এটি অ্যাপটি চালু করার সাথে সাথে মোবাইলের ভেতরে একটি লোকাল ফ্লাস্ক সার্ভার (`http://127.0.0.1:5000/`) ব্যাকগ্রাউন্ড থ্রেডে রান করবে। এরপর অ্যান্ড্রয়েডের নেটিভ **WebView** ব্যবহার করে সেই লোকাল পেজটি অ্যাপের স্ক্রিনে দেখাবে।
* **`buildozer.spec`:** এটি অ্যান্ড্রয়েড অ্যাপ তৈরি করার কনফিগারেশন ফাইল, যেখানে অ্যাপের নাম, পারমিশন এবং প্রয়োজনীয় পাইথন প্যাকেজগুলো ডিফাইন করা আছে।

---

## 🛠️ ধাপে ধাপে এপিকে (APK) তৈরি করার পদ্ধতি

যেহেতু লোকাল পাইথন কোড অ্যান্ড্রয়েডে রান করার জন্য বিল্ড টুলস (যেমন: Android SDK, NDK, Java, GCC) প্রয়োজন এবং এগুলো উইন্ডোজে সেটআপ করা খুব কঠিন, তাই আমরা **গুগল কোলাব (Google Colab)** ব্যবহার করে সম্পূর্ণ ব্রাউজার থেকেই সম্পূর্ণ ফ্রিতে অ্যাপটি কম্পাইল করব।

### ধাপ ১: আপনার কোড গিটহাবে (GitHub) আপলোড করা
1. [GitHub](https://github.com/)-এ যান এবং একটি ফ্রি অ্যাকাউন্ট তৈরি করুন।
2. একটি নতুন **Public Repository** তৈরি করুন (যেমন: `markitdown-offline-app`)।
3. আপনার পিসির সম্পূর্ণ প্রজেক্ট ডিরেক্টরিটি (যার মধ্যে `main.py`, `buildozer.spec`, `webapp`, এবং `packages` ফোল্ডার রয়েছে) এই গিটহাব রিপোজিটরিতে আপলোড বা পুশ করুন।

### ধাপ ২: Google Colab ওপেন করা
1. ব্রাউজারে [Google Colab](https://colab.research.google.com/)-এ যান এবং আপনার গুগল অ্যাকাউন্ট দিয়ে লগইন করুন।
2. **New Notebook**-এ ক্লিক করে একটি নতুন ফাইল তৈরি করুন।

### ধাপ ৩: কোলাবে বিল্ড কমান্ডগুলো রান করা
নিচের কোড ব্লকগুলো একটি একটি করে কোলাব সেলে (Cell) কপি করুন এবং পাশে থাকা **Play Button**-এ ক্লিক করে রান করুন:

#### ১. বিল্ডের জন্য প্রয়োজনীয় অ্যান্ড্রয়েড টুলস ও জাভা ইন্সটল করা:
```bash
# সিস্টেমের প্যাকেজ লিস্ট আপডেট করা এবং অ্যান্ড্রয়েড অ্যাপ কম্পাইল করার জন্য প্রয়োজনীয় টুলস সেটআপ করা
!apt-get update
!apt-get install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
# পাইথনের বিল্ড লাইব্রেরি Cython এবং Buildozer ইন্সটল করা
!pip install --upgrade Cython==0.29.33 virtualenv buildozer
```

#### ২. আপনার গিটহাব প্রজেক্ট ক্লোন করা:
*(নিচের লিঙ্কে আপনার গিটহাবের রিপোজিটরি লিঙ্কটি বসিয়ে দিন)*
```bash
# গিটহাব থেকে আপনার কোড ডাউনলোড করা হচ্ছে
!git clone https://github.com/আপনার-ইউজারনেম/markitdown-offline-app.git
# ক্লোন করা ফোল্ডারের ভেতরে প্রবেশ করা হচ্ছে
%cd markitdown-offline-app
```

#### ৩. এপিকে (APK) ফাইল বিল্ড করা:
```bash
# বিল্ডোজার দিয়ে অ্যান্ড্রয়েড এপিকে কম্পাইল করা শুরু করা
!buildozer android debug
```
> **নোট:** এই কমান্ডটি রান করার পর আপনাকে একটি টার্মস অ্যান্ড কন্ডিশন অ্যাকসেপ্ট করতে বলতে পারে, সেখানে কিবোর্ডের `y` লিখে এন্টার চাপুন। প্রথমবার অ্যাপ তৈরি হতে **১০ থেকে ১৫ মিনিট** সময় লাগতে পারে কারণ এটি অ্যান্ড্রয়েড SDK এবং NDK অটোমেটিক ডাউনলোড করে নেবে।

---

## 📥 ধাপ ৪: অ্যাপটি ফোনে ডাউনলোড ও ইন্সটল করা

1. বিল্ড শেষ হলে গুগল কোলাবের বামপাশের মেনু থেকে **Files** আইকনে ক্লিক করুন।
2. `markitdown-offline-app` ফোল্ডারের ভেতরে প্রবেশ করুন।
3. সেখানে `bin` নামের একটি ফোল্ডার দেখতে পাবেন। তার ভেতরে আপনার তৈরি করা এপিকে ফাইলটি থাকবে (যেমন: `MarkItDownConverter-1.0-debug.apk`)।
4. ফাইলের পাশে থাকা তিনটি ডটে ক্লিক করে **Download** করুন।
5. এপিকে ফাইলটি আপনার অ্যান্ড্রয়েড ফোনে নিয়ে ইন্সটল করুন। 

*(যেহেতু এটি গুগল প্লে স্টোরের বাইরে থেকে ইন্সটল করা হচ্ছে, তাই ফোন থেকে 'Install from Unknown Sources' অন করতে হতে পারে)*

---

## 📌 ফ্রিতে অ্যাপটি ছড়িয়ে দেওয়া (Distribution)

যেহেতু আপনি প্লে স্টোরের ২৫ ডলার ফি ছাড়া ফ্রিতে এটি সবাইকে দিতে চান:
1. **GitHub Releases:** আপনার গিটহাব রিপোজিটরির 'Releases' সেকশনে এপিকে ফাইলটি আপলোড করে রাখতে পারেন এবং সবাইকে সেই লিঙ্কটি দিতে পারেন।
2. **Amazon Appstore:** এখানে অ্যাপ পাবলিশ করা সম্পূর্ণ ফ্রি। [Amazon Developer Portal](https://developer.amazon.com/)-এ গিয়ে একটি ফ্রি অ্যাকাউন্ট খুলে আপনার এপিকেটি সাবমিট করতে পারেন।
3. **নিজের পোর্টফোলিও ওয়েবসাইট:** আপনার পোর্টফোলিও ওয়েবসাইট [samnahid21.web.app](https://samnahid21.web.app/)-এ একটি ডাউনলোড বাটন তৈরি করে সরাসরি গুগল ড্রাইভ বা গিটহাবের ডাউনলোড লিঙ্ক যুক্ত করে দিন।

কোনো সমস্যা হলে বা কোনো এরর আসলে আমাকে স্ক্রিনশট বা কোডটি দিন, আমি সমাধান করে দেব! 🎉
